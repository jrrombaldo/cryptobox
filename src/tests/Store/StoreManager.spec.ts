const expect = require("chai").expect;
// const store = require("../../services/store/StoreManager");

// TODO failing to mock electron

// const objStore = '🦄';
// const objKey = 'testing.unicorn'

// describe("service/store/StoreManager", () => {
//   describe("store()", () => {
//     it("creating", () => {
//       const result = store.set(objKey, objStore)
//       expect(result).to.null()
//     });

//     it("reading", () => {
//       const result = store.get(objKey)
//       expect(result).to.eql([]);
//     });

//     it("deleting", () => {
//       const result = store.delete(objKey)
//       expect(result).to.undefined()
//     });

//     it("reading after delete", () => {
//       const result = store.get(objKey)
//       expect(result).to.undefined()
//     });
//   });
// });

